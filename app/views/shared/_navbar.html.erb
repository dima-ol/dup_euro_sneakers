<nav class="w-full top-0 bg-zinc-950 z-10 py-4 border-b border-orange-500">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">
      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-between">
        <div class="hidden sm:flex items-center justify-center">
          <div class="flex space-x-10">
            <%= link_to 'Магазин', root_path, class: "text-orange-500 hover:underline py-2 rounded-md text-md font-medium" %>
          </div>
        </div>
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path do %>
            <%= image_tag 'logo.svg', class: 'block lg:hidden h-20 w-auto' %>
          <% end %>
          <%= link_to root_path do %>
            <%= image_tag 'logo.svg', class: 'hidden lg:block h-20 w-auto' %>
          <% end %>
        </div>
        <div class="hidden sm:flex sml:ml-6 items-center justify-center">
          <div class="flex space-x-10">
            <%= link_to cart_path, class: "text-orange-500 hover:underline py-2 rounded-md text-md font-medium" do %>
              <%= icon 'fa-solid', 'shopping-cart' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
